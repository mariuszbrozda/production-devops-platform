apiVersion: v1
kind: Service
metadata:
  name: {{ include "online-store.fullname" . }}
  labels:
    app: {{ include "online-store.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
    - port: 9090
      targetPort: 9090
      protocol: TCP
      name: metrics
  selector:
    app: {{ include "online-store.name" . }}
